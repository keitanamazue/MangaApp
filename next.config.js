/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  image: ["thumbnail.image.rakuten.co.jp", "cover.open.kobo.com"],
}

module.exports = nextConfig
